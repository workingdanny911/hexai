import { describe, expect, it, test } from "vitest";

describe("IdempotentReceiver", () => {
    test("test", () => {
        expect(1).toBe(1);
    });

    it("test", () => {
        expect(1).toBe(1);
    });
});
